apiVersion: v1
kind: Service
metadata:
  name: gcp-platform-service
spec:
  type: LoadBalancer

  selector:
    app: gcp-platform-app

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080