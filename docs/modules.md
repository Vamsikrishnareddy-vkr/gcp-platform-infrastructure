# Terraform Modules

I split the Terraform configuration into separate modules so I can reuse the same infrastructure code for different environments.

Each module has:

```text
main.tf
variables.tf
outputs.tf